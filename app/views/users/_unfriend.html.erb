<%= form_for(current_user.active_friend_requests.find_by(requested_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfriend", class: "btn" %>
<% end %>