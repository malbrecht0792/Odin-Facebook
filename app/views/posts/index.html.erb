<h1>Posts#index</h1>

<% @posts.each do |post| %>
	<p>
		<%= post.author.first_name %>
		<%= post.author.last_name %>
		<%= post.created_at %>
	</p>
	<p>
		Content:
		<%= post.content %>
	</p>
<% end %>